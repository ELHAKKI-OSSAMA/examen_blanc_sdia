spring.application.name=config-service

server.port=8088
# Depot Git de configuration : surchargeable via la variable CONFIG_REPO_URI.
# Par defaut, un dossier "config-repo" dans le repertoire personnel de l'utilisateur.
spring.cloud.config.server.git.uri=${CONFIG_REPO_URI:file://${user.home}/config-repo}